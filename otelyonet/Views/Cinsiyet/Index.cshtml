@model IEnumerable<otelyonet.Models.Cinsiyet>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CinsiyetAdı)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CinsiyetAdı)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CinsiyetID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CinsiyetID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CinsiyetID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
