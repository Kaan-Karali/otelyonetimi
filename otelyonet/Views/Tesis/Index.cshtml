@model IEnumerable<otelyonet.Models.Tesis>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TesisAdı)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Açıklama)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TesisAdı)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Açıklama)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TesisID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TesisID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TesisID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
